---
title: v2.1.4.0 版本更新
tags: [更新笔记, 2.1.4.0]
---

## 本体修改

- 尝试修复插件在部分环境下加载异常的问题
- 修复了 `高脚孤丘 → 乌尔达哈来生回廊` 路径错误的问题
- 通过插件内打开地图标点将会自动聚焦在标点上
- 极大优化了插件重载速度
    - 模块加载/卸载改为动态计算最优并行配置
    - 密集优化了一大批模块实例化时间
    - 将联网请求移出了初始加载
    - 批量加载/卸载模块改为分批在 Framework 统一执行, 减少线程切换
- 为模块列表新增 `紧凑模式`, 启用后, 全部模块将仅只绘制启用勾选框、模块标题、模块描述, 并统一维持一行的高度, 在模块列表内容区右上角可切换两种模式

![CompactView](/assets/Changelog/2.1.4.0/CompactView.png)

## 新增模块

### 自动高亮冲刺 [技能]

*推荐模块*

进入副本后, 若当前未在战斗状态, 则自动高亮 "冲刺" 技能

### 自动灵极魂 [技能]

若当前职业为 "黑魔法师", 则在进入副本、触发重新挑战、脱战后, 自动使用 "灵极魂"

### 自动显示藏宝图位置 [系统]

*推荐模块*

解读/使用藏宝图时, 自动打开地图并标记详细位置

### 自定义地图标记 [界面]

*推荐模块*

为游戏原生地图添加自定义标记, 支持备注、分组、批量导入导出等

![CustomizeMapMarker](/assets/Changelog/2.1.4.0/CustomizeMapMarker.png)

## 模块修改

### 玩家目标情报扩展 [界面]

- 新增参数 `/Title/`, 用以显示玩家佩戴的称号

### 自动登录 [系统]

- 修复了在大区内跨服后会以角色当前所在服务器搜寻角色导致流程失败的问题

### 自动显示网络延迟 [界面]

- 优化了延迟获取方式, 使获取的延迟更加真实
- 修复了部分情况下会持续返回 100% 丢包率的问题
- 极大精简了模块逻辑
- 在 Wine 环境下不再尝试执行 Win32 API 相关的 RTT 获取

### 自动开始临危受命任务 [战斗]

- 极大精简了模块逻辑, 并提升了性能表现

### 自动高亮状态对应技能 [技能]

- 简化了模块逻辑

### 快捷雇员物品存取 [界面]

- 修复了缺失本地化键的问题

### 随机任务：指导者任务记录上传助手 [副本]

- 修复了可能上传未知的职业信息的问题

### 自动显示物品 NPC 商店信息 [界面]

- 优化了去向界面部分文本节点的显示逻辑

### 更好的市场布告板 [界面]

- 为服务器选择组件新增右键单击回到当前服务器的功能

### 自动游玩重击伽美什 [金碟]

- 优化精简了模块逻辑

### 自动游玩强袭水晶塔 [金碟]

- 完全重构了模块逻辑

### 自动游玩莫古抓球机 [金碟]

- 完全重构了模块逻辑
- 调整了模块内部名称, 需要重新启用

### 更好的自动跟随 [系统]

- 完全重构了模块逻辑
- 新增明确的跟随、暂停、等待目标等状态, 目标暂时消失或无法选中时会等待目标重新出现并自动续跟
- 手动移动、使用技能、读条、过图、观看剧情等情况下会暂停跟随, 条件恢复后自动继续
- 手动移动结束后会等待一段时间再按照目标最新位置重新寻路
- 新增跟随距离缓冲区, 减少在临界距离附近反复停止和重新跟随的问题
- 优化 vnavmesh 跟随, 支持近距离寻路、路径更新、停止查询和寻路失败时回退至游戏原生跟随
- 右键菜单现在会根据当前状态显示开始跟随、切换跟随目标或停止跟随
- 指令新增 `/pdrfollow stop`, 用于主动停止当前跟随
- 优化了与目标一同上坐骑、起飞和下坐骑的执行逻辑
- 修复了距离检查周期不准确、目标不可选中时反复续跟等问题

### 敌对列表界面优化 [战斗]

- 优化了状态效果组件的显示位置